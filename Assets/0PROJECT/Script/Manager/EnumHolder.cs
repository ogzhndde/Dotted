/// <summary>   
/// Keeps all the enum types in the game in one class.
/// </summary>
public enum DotType
{
    Standart,
    TimeBomb
}

public enum ParticleType
{
    Explode,
    EarnScore,
    Intersection
}


